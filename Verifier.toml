# Public inputs used during `nargo verify`.
# Values must match those in Prover.toml used to generate the proof.

# public_puzzle_hash: the Poseidon/BN254 hash of the secret, stored on-chain.
public_puzzle_hash = "0"

# contributor_wallet: the claimant's Stellar address encoded as a field element.
contributor_wallet = "0"
