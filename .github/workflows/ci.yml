name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  circuit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache Nargo binary
        uses: actions/cache@v4
        with:
          path: ~/.nargo
          key: ${{ runner.os }}-nargo-0.30.0

      - name: Install Nargo
        run: |
          if ! command -v nargo &>/dev/null; then
            curl -L https://raw.githubusercontent.com/noir-lang/noirup/main/install | bash
            export PATH="$HOME/.noirup/bin:$HOME/.nargo/bin:$PATH"
          fi
          nargo --version

      - name: Pin Nargo version
        run: |
          export PATH="$HOME/.noirup/bin:$HOME/.nargo/bin:$PATH"
          noirup --version 0.30.0

      - name: Compile circuit
        run: |
          export PATH="$HOME/.noirup/bin:$HOME/.nargo/bin:$PATH"
          nargo compile

      - name: Run tests
        run: |
          export PATH="$HOME/.noirup/bin:$HOME/.nargo/bin:$PATH"
          nargo test

      - name: Generate proof (smoke test)
        run: |
          export PATH="$HOME/.noirup/bin:$HOME/.nargo/bin:$PATH"
          nargo prove || echo "Skipped — Prover.toml uses placeholder values"

      - name: Verify proof (smoke test)
        run: |
          export PATH="$HOME/.noirup/bin:$HOME/.nargo/bin:$PATH"
          nargo verify || echo "Skipped — no proof artifact from placeholder inputs"
